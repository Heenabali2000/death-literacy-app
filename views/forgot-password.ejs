<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Forgot Password - TechShift</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #0b0b0b 0%, #666666 100%);
      margin: 0;
      padding: 0;
      color: #333;
    }

    nav {
      background-color: #000;
      padding: 10px 20px;
      display: flex;
      align-items: center;
    }

    nav img {
      height: 40px;
      margin-right: 10px;
    }

    nav span {
      color: white;
      font-size: 20px;
      font-weight: bold;
    }

    .container {
      background-color: #fff;
      padding: 2.5em;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      width: 100%;
      max-width: 420px;
      margin: 60px auto;
      text-align: center;
    }

    h2 {
      color: #758176;
      margin-bottom: 1em;
    }

    label {
      text-align: left;
      display: block;
      margin: 10px 0 5px;
      font-weight: 500;
    }

    input {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 6px;
      margin-bottom: 1em;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      background-color: #1f6e21;
      color: #fff;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
    }

    button:hover {
      background-color: #1f6e21;
    }

    .message {
      padding: 10px;
      background-color: #e7f3ff;
      border: 1px solid #b3d7ff;
      color: #004085;
      border-radius: 5px;
      margin-bottom: 1em;
      font-size: 0.95rem;
    }

    .link {
      margin-top: 1em;
      font-size: 0.9rem;
    }

    .link a {
      color: #133114;
      text-decoration: none;
    }

    .link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <nav>
    <img src="/images/logo.png" alt="Logo" />
    <span>TechShift</span>
  </nav>

  <div class="container">
    <h2>Forgot Password</h2>

    <% if (message) { %>
      <div class="message"><%= message %></div>
    <% } %>

    <form method="POST" action="/forgot-password">
      <label for="email">Enter your email address</label>
      <input type="email" id="email" name="email" required placeholder="you@example.com" />
      <button type="submit">Send Reset Link</button>
    </form>

    <div class="link">
      <p><a href="/login">← Back to Login</a></p>
    </div>
  </div>

</body>
</html>


