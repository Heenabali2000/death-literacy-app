<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sign Up - TechShift</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #0f0f0f 0%, #666666 100%);
      margin: 0;
      padding: 0;
      color: #333;
    }

    nav {
      background-color: #121212;
      padding: 10px 20px;
      display: flex;
      align-items: center;
    }

    nav img {
      height: 40px;
      margin-right: 10px;
    }

    nav span {
      color: white;
      font-size: 20px;
      font-weight: bold;
    }

    .container {
      background-color: #fff;
      padding: 2.5em;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      width: 100%;
      max-width: 420px;
      margin: 60px auto;
      text-align: center;
    }

    h2 {
      color: #333;
      margin-bottom: 1em;
    }

    input {
      width: 100%;
      padding: 12px;
      margin: 8px 0;
      border-radius: 6px;
      border: 1px solid #ccc;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      background-color: #1f6e21;
      color: #fff;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
    }

    button:hover {
      background-color: #155c17;
    }

    .nav-links {
      margin-top: 20px;
      font-size: 0.9rem;
    }

    .nav-links a {
      color: #1f6e21;
      text-decoration: none;
    }

    .error {
      color: red;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>

  <nav>
    <img src="/images/logo.png" alt="Logo" />
    <span>TechShift</span>
  </nav>

  <div class="container">
    <h2>Create an Account</h2>

    <% if (error_msg) { %>
      <div class="error"><%= error_msg %></div>
    <% } %>

    <form method="POST" action="/signup">
      <input type="text" name="firstname" placeholder="First Name" value="<%= formData.firstname || '' %>" required>
      <input type="text" name="lastname" placeholder="Last Name" value="<%= formData.lastname || '' %>" required>
      <input type="text" name="organization" placeholder="Organization" value="<%= formData.organization || '' %>">
      <input type="email" name="email" placeholder="Email" value="<%= formData.email || '' %>" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Sign Up</button>
    </form>

    <div class="nav-links">
      Already have an account? <a href="/login">Login</a>
    </div>
  </div>
</body>
</html>



